F=${1%%asm}
E=macro11.errs
../macro11 -yus -ysl 16 -o ${F}obj -l ${F}lst $1 2>$E || echo Errors found >> $E
if [ -s $E ]; then cat $E; rm $E; exit 8; else rm -f $E; fi && \
../dumpobj ${F}obj >${F}dump && \
./obj2bin <${F}dump >${F}bin && \
awk -f lst2hex.awk ${F}lst > ${F}hex.lst
